diff --git a/analytics/metrics.py b/analytics/metrics.py
new file mode 100644
--- /dev/null
+++ b/analytics/metrics.py
@@ -0,0 +1,40 @@
+# Metrics calculation module
+
+def calc(data):
+    # poor naming: unclear what 'calc' does or what it returns
+    r = []
+    for i in range(len(data)):   # use enumerate() or direct iteration instead
+        x = data[i]
+        r.append(x * 2)
+    return r
+
+def get_avg(nums):
+    total = 0
+    for n in nums:
+        total = total + n        # use sum(nums) instead
+    return total / len(nums)     # BUG: ZeroDivisionError if nums is empty
+
+def process_batch(items, multiplier=1.0):
+    out = []
+    for item in items:
+        if item > 0:
+            out.append(item * multiplier)
+        # BUG: negative values silently dropped with no warning or logging
+    return out
+
+def merge_dicts(d1, d2):
+    result = {}
+    for k in d1:
+        result[k] = d1[k]
+    for k in d2:
+        result[k] = d2[k]
+    return result
+    # Redundant: use {**d1, **d2} or d1 | d2 (Python 3.9+)
+
+class DataProcessor:
+    def __init__(self):
+        self.data = []
+        self.processed = False
+
+    def add(self, item):
+        self.data.append(item)
+
+    def run(self):
+        self.data = calc(self.data)
+        self.processed = True
+        # BUG: no return value — caller has no way to retrieve results
